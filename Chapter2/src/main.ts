let myName: string = "Rikas";
let meaningOfLife: number;
let isLoading: boolean;
let album: any;

myName = "Mohomed";
meaningOfLife = 25;
isLoading = true;
album = 5150;

const sunm = (a: number, b: number) => {
  return a + b;
};

let postId: string | number;
let isActive: number | boolean;

let re: RegExp = /\w+/g;
